import React from 'react';
import renderer from 'react-test-renderer';
import { LockedEntryCard } from 'components';

describe('LockedEntryCard component', () => {
  // beforeEach(() => {});
  // afterEach(() => {});
  it('says hello', () => {
    expect(true).toBe(true);
  })
});
