'SET_LOGIN_STATE'
'GET_LOGIN_STATE'

const userReducer = (state, action) => {
  switch (action.type) {
    case 'SET_LOGIN_STATE':

    case 'GET_LOGIN_STATE':

    default:
      return state;
 }
}

export default userReducer;
